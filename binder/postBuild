# installation de lumpy-sv, qui ne fonctionne pas depuis le yml
#pip install lumpy-sv

# The VEP package installs only the variant effect predictor (VEP) library code.
# install the the VEP library for human GRCh38 to a directory created in the env

vep_install -a cf -s homo_sapiens -y GRCh38 -c /GRCh38/vep --CONVERT

# note that vep_install is renamed from INSTALL.pl to avoid having generic script names in the PATH
# The --CONVERT flag is not required but improves lookup speeds during runs.
# See the VEP documentation for more details
